from .database import Base, engine
from .models import Card

# No need to create tables as they already exist in PasaraEats database
# But keeping this file for consistency and potential future models